html
  head
    link(rel="icon", href="/favicon.ico?v=1")
    link(rel="stylesheet", href="/style.css")
  body
    h1 Users
    ul
      each u in users
        li: a(href=`/users/${u.id}`)= u.username